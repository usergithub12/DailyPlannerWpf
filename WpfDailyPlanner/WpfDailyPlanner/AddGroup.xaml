<Window x:Class="WpfDailyPlanner.AddGroup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDailyPlanner"
        mc:Ignorable="d"
        Title="AddGroup" Height="149.529" Width="426.176"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStartupLocation="CenterOwner"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="15"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <GroupBox Header="Type name to add">
            <StackPanel>
                <TextBox
  x:Name="tb_GroupName"
  materialDesign:HintAssist.Hint="Group Name" />
                <Button Name="btn_confirm" Click="Btn_confirm_Click" >Confirm</Button>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
