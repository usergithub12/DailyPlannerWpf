<Window x:Class="WpfDailyPlanner.DailyTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDailyPlanner"
        mc:Ignorable="d"
        Title="DailyTask" Height="450" Width="800">
    <Grid Background="Gray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Calendar Name="cl_main" />

            <Label Name="lb_today">Today:</Label>
            <Label>Task Name:</Label>
            <TextBox Name="tb_taskname"></TextBox>
            <Label>Description</Label>
            <TextBox TextWrapping="Wrap"
                AcceptsReturn="True"
                VerticalScrollBarVisibility="Visible" Name="tb_taskdescr"></TextBox>

        </StackPanel>
        <StackPanel Grid.Column="1">
            <Button Name="btn_add">Add task</Button>
            <Button Name="btn_delete">Delete task</Button>
            <Button Name="btn_save">Save</Button>
        </StackPanel>
    </Grid>
</Window>
